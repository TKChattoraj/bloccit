
<h1>New Question</h1>

<div class="row">
  <div class="col-md-4">
    <p>Guidelens for Questions</p>
    <ul>
      <li>Make sure it pithy.</li>
      <li>Don't use the letter "Q."</li>
      <li>The iincessant use of hastags will get you banned.</li>
    </ul>
  </div>
  <div class="col-md-8">
    <%= form_for @question do |f| %>
      <div class="form-group">
        <%= f.label :title %>
        <%= f.text_field :title, class: 'form-control', placeholder: "Enter question title" %>
      </div>
      <div class="form-group">
        <%= f.label :body %>
        <%= f.text_field :body, rows: 8, class: 'form-control', placeholder: "Enter question" %>
      </div>
      <div class="form-group">
        <%= f.submit "Save", class: 'btn-success' %>
      </div>
    <% end %>
  </div>
</div>
